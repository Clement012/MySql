use sys;


---
-- foreign key(protection in table) <- many
-- // not only one
-- -> have to add data in foreign key first, if no maybe add failed
-- primary key <- one
-- candidate key <- sub
-- super key <- a+b maybe primary key

-- constraint 

-- one to one
-- indexing -> with different column data -> reducing searching speed
-- readabitility (權限/可視度)

-- one to many 
-- 

-- many to many
-- 

-- subquery
-- --> correlated subquery**
-- --> CTE
-- exists 
-- > similarly to inner join but only select one column only
-- not exists 
-- > join but can sort [not exist] data from table 
-- join